import { Request, Response, NextFunction } from 'express';

export const signIn = (
  request: Request,
  response: Response,
  next: NextFunction,
) => {
  request;
  response;
  next;
};

export const signUp = (
  request: Request,
  response: Response,
  next: NextFunction,
) => {
  request;
  response;
  next;
};
